# Normally use npm start: it detects the local backend automatically.
# Optional override for direct Expo commands; /api is added automatically.
# EXPO_PUBLIC_API_URL=http://192.168.1.100:4000
# For production, run npm run connect -- production --url https://your-backend-domain
# Never put secrets in app configuration or EXPO_PUBLIC variables.
