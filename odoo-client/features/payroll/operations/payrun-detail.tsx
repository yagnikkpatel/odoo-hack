'use client'
import DataConnectionNotice from '@/features/hr/components/data-connection-notice'
import { useState } from 'react'
import { useRouter } from 'next/navigation'
import { CalculatorIcon, CheckIcon, SendIcon, WalletIcon, TrashIcon } from 'lucide-react'
import { Button } from '@/features/nexacrm/components/ui/button'
import { Card } from '@/features/nexacrm/components/ui/card'
import { Input } from '@/features/nexacrm/components/ui/input'
import RecordNotFound from '@/features/nexacrm/components/record/record-not-found'
import { EditorDialog, FormField } from '@/features/hr/components/form'
import { usePayrollStore } from '../store'
import { usePayrollPermissions } from '../permissions'
import { money, type PayrollResult } from '../types'
import DeliveryDialog from '../delivery-dialog'
import PayrunWizard from './wizard'
import { Heading, Status, Warnings, SlipTable, AccessDenied } from './shared'
export default function PayrunDetail({id}:{id:string}){
 const router=useRouter();const store=usePayrollStore(s=>s);const run=store.payruns.find(r=>r.id===id);const slips=store.payslips.filter(s=>s.payrunId===id);const {canRead,canProcess,canDelete}=usePayrollPermissions();const [error,setError]=useState<string|null>(null);const [delivery,setDelivery]=useState(false);const [editing,setEditing]=useState(false);const [confirm,setConfirm]=useState<'validate'|'paid'|'delete'|null>(null);const [bank,setBank]=useState<{id:string;name:string;account:string}|null>(null)
 const action=(result:PayrollResult)=>{setError(result.ok?null:result.error);return result.ok}
 if(!canRead)return <AccessDenied/>;if(!run)return <RecordNotFound label="Payrun" backHref="/payroll" backLabel="Payruns"/>
 const locked=run.status==='validated'||run.status==='paid';const totals=Array.from(new Set(slips.map(s=>s.currency))).map(currency=>({currency,net:slips.filter(s=>s.currency===currency).reduce((sum,s)=>sum+s.net,0),gross:slips.filter(s=>s.currency===currency).reduce((sum,s)=>sum+s.gross,0)}))
 return <div className="flex min-h-full flex-col"><Heading title={run.name} back="/payroll"><Status status={run.status}/>{canDelete&&!locked&&<Button size="icon-sm" variant="ghost" aria-label="Delete draft payrun" onClick={()=>setConfirm('delete')}><TrashIcon/></Button>}</Heading><div className="space-y-5 py-4"><DataConnectionNotice /><div className="flex flex-wrap gap-2">{canProcess&&<>{!locked&&<Button size="sm" variant="outline" onClick={()=>setEditing(true)}>Edit payrun</Button>}<Button size="sm" variant="outline" disabled={locked} onClick={()=>action(store.computePayrun(id))}><CalculatorIcon/>{run.status==='computed'?'Recompute':'Compute'}</Button><Button size="sm" variant="outline" disabled={run.status!=='computed'} onClick={()=>setConfirm('validate')}><CheckIcon/>Validate</Button><Button size="sm" disabled={run.status!=='validated'} onClick={()=>setConfirm('paid')}><WalletIcon/>Mark paid</Button><Button size="sm" variant="outline" disabled={!locked} onClick={()=>setDelivery(true)}><SendIcon/>Send payslips</Button></>}</div>{error&&<p role="alert" className="text-sm text-destructive">{error}</p>}<Card className="grid gap-4 p-4 sm:grid-cols-2 lg:grid-cols-4">{[['Salary structure',run.structureName],['Payroll period',`${run.startDate} – ${run.endDate}`],['Employees',String(run.employeeIds.length)],['Total net',totals.length?totals.map(t=>money(t.net,t.currency)).join(' + '):'Awaiting computation']].map(([label,value])=><div key={label}><p className="mb-1 text-xs text-muted-foreground">{label}</p><p className="text-sm font-medium">{value}</p></div>)}</Card>{locked&&<p className="rounded-md border bg-muted/30 px-4 py-3 text-sm">{run.status==='paid'?`Payment recorded ${run.paidAt?new Date(run.paidAt).toLocaleString():''}.`:'This payrun is validated and ready for payment.'} Salary and contract snapshots are locked for payroll history.</p>}<Warnings warnings={run.warnings.map(warning=>({...warning,message:warning.employeeId?`${slips.find(s=>s.employeeId===warning.employeeId)?.employeeName||'Employee'}: ${warning.message}`:warning.message}))}/>{canProcess&&!locked&&slips.some(s=>!store.bankDetails[s.employeeId])&&<Card className="gap-3 p-4"><h2 className="text-sm font-semibold">Employee bank details</h2><p className="text-xs text-muted-foreground">Add the missing bank account, then recompute to refresh validation checks.</p><div className="flex flex-wrap gap-2">{slips.filter(s=>!store.bankDetails[s.employeeId]).map(s=><Button key={s.id} variant="outline" size="sm" onClick={()=>{setError(null);setBank({id:s.employeeId,name:s.employeeName,account:''})}}>Add bank details · {s.employeeName}</Button>)}</div></Card>}<div><h2 className="mb-3 text-sm font-semibold">Payslips <span className="font-normal text-muted-foreground">({slips.length})</span></h2><Card className="gap-0 overflow-hidden py-0"><SlipTable slips={slips}/></Card>{run.status==='draft'&&<p className="mt-3 text-xs text-muted-foreground">Compute applies the salary rules and period contract to each selected employee.</p>}</div></div>{editing&&<PayrunWizard run={run} onClose={()=>setEditing(false)}/>} {delivery&&<DeliveryDialog run={run} onClose={()=>setDelivery(false)}/>} {confirm&&<EditorDialog title={confirm==='delete'?'Delete payrun?':confirm==='paid'?'Record payroll payment?':'Validate payroll?'} description={confirm==='delete'?'This removes the unfinalized batch and its payslips.':confirm==='paid'?'Record that payment has been completed outside this application. This action does not transfer money.':'Validation locks the salary breakdown and contract snapshots as payroll history. Resolve all blocking warnings first.'} submitLabel={confirm==='delete'?'Delete payrun':confirm==='paid'?'Mark paid':'Validate payrun'} onClose={()=>setConfirm(null)} onSubmit={e=>{e.preventDefault();const success=action(confirm==='delete'?store.removePayrun(id):confirm==='paid'?store.markPaid(id):store.validatePayrun(id));if(success){if(confirm==='delete')router.push('/payroll');setConfirm(null)}else setConfirm(null)}}><p className="text-sm">{run.name} · {run.employeeIds.length} employees</p></EditorDialog>}{bank&&<EditorDialog title={`Bank details · ${bank.name}`} description="Save the employee account used for payroll validation." error={error} onClose={()=>setBank(null)} onSubmit={e=>{e.preventDefault();if(action(store.setBankDetails(bank.id,bank.account)))setBank(null)}}><FormField label="Bank account / IBAN" id="payroll-bank"><Input id="payroll-bank" required autoComplete="off" value={bank.account} onChange={e=>setBank({...bank,account:e.target.value})}/></FormField></EditorDialog>}</div>
}
