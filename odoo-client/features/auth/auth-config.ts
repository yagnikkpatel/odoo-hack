// Temporary UI preview: set to false when backend authentication is ready.
// Keep dashboard data mocked while this is enabled.
export const authConfig = {
  previewEnabled: true,
  previewUser: {
    email: "preview@peoplepay360.local",
    role: "ADMIN",
  },
};
